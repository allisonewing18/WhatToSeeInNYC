const firstName = "Allison";
const lastName = "Ewing;"

function fullName(firstName, lastName) {
    return fullname = firstName + " " + lastName;
}
console.log(fullname);

const array = [1, 2, 3, 4, 5];
let count = 0;
function mean(array)
{
    for (let i =0; i<array.length;i++) 
    {
        count+= array[i];
    }
    return Mean = count/array.length;
}
console.log(Mean);

let integer = Math.random();
function numberprint(integer) 
{
    for (let i =1; i<=integer; i++)
    {
        console.log(" ");
        for (let j=1;j<=integer;j++)
        {
            console.log(j);
        }
    }
}
console.log(numberprint);